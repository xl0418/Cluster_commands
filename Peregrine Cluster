# edit file
nano *.*

# read file
cat *.*

#check jot queue
squeue -u $USER

# run script
sbatch *.sh

# install packages
install.packages("DDD")

# installed packages
The downloaded source packages are in
	'/tmp/Rtmpceb77j/downloaded_packages'
	
# search "as" on cluster	
egrep -iR "as" --include=~/R/x86_64-pc-linux-gnu-library/3.2/expoRkit/R/*.R
